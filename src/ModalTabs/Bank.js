import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Bank = () => {
  return (
    <View>
      <Text>Bank</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Bank;
