import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Call = () => {
  return (
    <View>
      <Text>Call</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Call;
